# Forms Reference

Use the document and spreadsheet tools to inspect fields, preserve existing values, validate required fields, and verify the saved result. Prefer explicit field mappings and report fields that cannot be filled safely.
