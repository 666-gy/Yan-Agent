# API Reference

A Skill package contains SKILL.md, optional agents/openai.yaml metadata, and optional scripts, references, and assets. Keep paths relative to the package and load resources only when the task requires them.
